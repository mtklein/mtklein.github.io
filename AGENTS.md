# Repository guidance

For requests to create or revise a short game in `oneshot/`, read and follow [the One Shot skill](.codex/skills/one-shot/SKILL.md). The collection is published directly from `main` as static GitHub Pages content. Each piece has its own HTML page linked from `oneshot/index.html`.
